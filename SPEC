{
    module_name    => 'Parse::HTTP::UserAgent',
    requires       => {
        'version'  => 0,
        ( $] < 5.006 ? (
            'warnings::compat' => 0,
        ) : ()),
    },
    build_requires => {
        'Test::More' => '0.40',
    },
    recommends => {},
    meta_merge => {
        resources => {
            repository => 'http://bitbucket.org/burak/cpan-parse-http-useragent/',
        },
    },
}
